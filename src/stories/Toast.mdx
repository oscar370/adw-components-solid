import {
	Meta,
	Title,
	Primary,
	Controls,
	Stories,
} from "@storybook/addon-docs/blocks";

<Meta isTemplate />
<Title />
<Primary />
<Controls />

## Notes

- You need to install `sonner`.
- You only need to declare the `Toaster` once, as high up in the layout as possible.
- To display a toast, use the `toast` function from `sonner`.

<Stories />
